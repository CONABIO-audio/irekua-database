import pytest


@pytest.mark.xfail
@pytest.mark.django_db
def test_collection_annotation_manager_open():
    """Should check if the open method of the object manager of
    CollectionAnnotation model is working properly"""
    assert False


@pytest.mark.xfail
@pytest.mark.django_db
def test_collection_annotation_manager_managed():
    """Should check if the managed method of the object manager of
    CollectionAnnotation model is working properly"""
    assert False


@pytest.mark.xfail
@pytest.mark.django_db
def test_collection_annotation_manager_administered():
    """Should check if the administered method of the object manager of
    CollectionAnnotation model is working properly"""
    assert False


@pytest.mark.xfail
@pytest.mark.django_db
def test_collection_annotation_manager_shared():
    """Should check if the shared method of the object manager of
    CollectionAnnotation model is working properly"""
    assert False


@pytest.mark.xfail
@pytest.mark.django_db
def test_collection_annotation_can_view():
    """Should check if the method can_view of the CollectionAnnotation model is
    working properly"""
    assert False


@pytest.mark.xfail
@pytest.mark.django_db
def test_collection_annotation_can_change():
    """Should check if the method can_change of the CollectionAnnotation model
    is working properly"""
    assert False


@pytest.mark.xfail
@pytest.mark.django_db
def test_collection_annotation_can_delete():
    """Should check if the method can_delete of the CollectionAnnotation model
    is working properly"""
    assert False


@pytest.mark.xfail
@pytest.mark.django_db
def test_collection_annotation_can_vote():
    """Should check if the method can_vote of the CollectionAnnotation model is
    working properly"""
    assert False
